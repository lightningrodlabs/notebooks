[package]
edition = "2021"
name = "notes_integrity"
version = "0.0.1"

[lib]
crate-type = ["cdylib", "rlib"]
name = "notes_integrity"

[dependencies]
chrono = {version = "0.4", features = ["serde"]}
derive_more = "0"
serde = "1"
regex = "1"

hdi = {version = "0.0.14"}
